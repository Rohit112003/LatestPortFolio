import React from 'react'

const TchIcon = ({component}: {component:React.ElementType}) => {
 const Component = component;
    return (
   <Component className ="size-10" />
  )
}

export default TchIcon
